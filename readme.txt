Python version : 3.8+

How to run:

1. Create a new environment and install the libraries*

2. Once you are into environment run main.py file with

(env) user@name $ python3 main.py

3. Exit with "ESC" key

*Libraries:
	opencv
	numpy

